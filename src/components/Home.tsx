"use client";

// import useTrans from "@/hooks/useTrans";

const Home =  (props: any) => {
  // const trans = useTrans()
  console.log("env:", process.env.NEXT_PUBLIC_ENV);

  return <> 'trans.home.content' </>;
};
export default Home;
